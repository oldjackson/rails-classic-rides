<div class="container col-sm-4 col-sm-offset-4">
   <h2>Add a new booking</h2>
<%= simple_form_for @booking, :url => car_bookings_path, :method => :post do |f| %>
  <div class="form-inputs">
    <%= f.date_field :start_date, required: true, include_blank: :true %>
    <%= f.date_field :end_date, required: true %>
  </div>
    <%= f.input :total_price, :label => "Personal message", as: :text,  required: true %>
  <div class="form-actions">
    <%= f.button :submit, "Book now" %>
  </div>
<% end %>
</div>

